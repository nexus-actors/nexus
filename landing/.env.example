# Override the docs site URL for local development.
# Defaults to https://docs.nexusactors.com when unset (production behaviour).
# PUBLIC_DOCS_URL=http://127.0.0.1:3000/docs

# Override the API reference site URL for local development.
# Defaults to https://api.nexusactors.com when unset.
# PUBLIC_API_URL=http://127.0.0.1:8081
