services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Actor\OrderProcessorActor:
        arguments:
            $transport: '@messenger.transport.orders'
