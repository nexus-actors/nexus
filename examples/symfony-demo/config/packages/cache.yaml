framework:
    cache:
        default_redis_provider: '%env(REDIS_DSN)%'
        pools:
            cache.app:
                adapter: cache.adapter.redis_tag_aware
                default_lifetime: 300
