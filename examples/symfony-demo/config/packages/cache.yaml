framework:
    cache:
        # Used as fallback for CLI and tests. In Swoole workers CoroutineLocalRedisCache
        # takes over via the CacheInterface alias in services.yaml.
        app: cache.adapter.filesystem
